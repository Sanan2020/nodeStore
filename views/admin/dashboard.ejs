<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>จัดการสินค้า</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        td,
        th {
            /* border: 1px solid #353434; */
            border: 1px solid #dddddd;
            padding: 8px;
        }

        th {
            text-align: center;
        }
    </style>
</head>

<body>
    <div class="container mt-5" style="margin-bottom: 50px;">
        <a class="btn btn-success" style="margin: 18px 0;" href="/insert">เพิ่มสินค้า</a>
        <button class="btn btn-secondary" style="margin: 18px 0;">การสั่งซื้อ</button>
        <table style=" border-collapse: collapse; width: 100%;">
            <tr>
                <th>รูปสินค้า</th>
                <th>ชื่อสินค้า</th>
                <th>ราคา</th>
                <th>รายละเอียด</th>
                <th>แก้ไข</th>
                <th>ลบ</th>
            </tr>
            <% Object.values(products).forEach(function(item){ %>
            <tr>
                <td>
                    <img src="/image/<%= item.image %>" alt="" width="100" height="80">
                </td>
                <td>
                    <%= item.name %>
                </td>
                <td>
                    <%= item.price %>
                </td>
                <td>
                    <%= item.description %>
                </td>
                <td>
                    <form action="/edit" method="post">
                        <input type="hidden" name="edit_id" value="<%=item._id%>">
                        <button type="submit" class="btn btn-warning">แก้ไข</button>
                    </form>
                </td>
                <td>
                    <a class="btn btn-danger" href="/delete/<%=item._id%>"
                        onclick="return confirm('คุณต้องการลบหรือไม่?')">ลบ</a>
                </td>
            </tr>
            <% }) %>
        </table>
    </div>

</body>
</html>