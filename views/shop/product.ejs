<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            /* display: flex; */

        }

        .card {
            /* box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2); */
            /* max-width: 600px; */
            /* margin: auto; */
            /* text-align: center; */
            font-family: arial;

            /* display: flex; */
            /* flex-direction: row; */
            /* margin: 0;
            padding: 0; */
        }

        .price {
            color: grey;
            font-size: 22px;
        }

        .card button {
            border: none;
            outline: 0;
            padding: 12px;
            color: white;
            background-color: #000;
            text-align: center;
            cursor: pointer;
            width: 100%;
            font-size: 18px;
        }

        .card button:hover {
            opacity: 0.7;
        }

        @media screen and (max-width: 767px) {
            .wid {
                /* max-width: 325px; */
            }
        }

        /* 1199 - 768 */
        /* @media (max-width: 1399px) and (min-width: 768px) {
            .im {
               width: 300px;
            }
        } */
    </style>

</head>

<body>
    <div class="container" style="margin-top: 136px; background: 0;">
        <% if(product){ %>
        <div class="card mt-4" style="background: 0;">
            <div class="row row-cols-1 row-cols-md-2">

                <div class="col-xl-4" style="background:#a7f398; display: flex; justify-content: center;">
                    <img class="wid im" src="/image/<%= product.image %>" alt="">
                </div>


                <div class="col-xl-8" style="background:#f0a8a8; display: flex; justify-content: center;">
                    <div class="card-body wid" style="background: 0; ">
                        <h2 style="background: 0;"><%= product.name %></h2>
                        <p><%= product.description %></p>
                        <p class="price">฿<%= product.price %></p>
                        <p><button style="max-width: 293px;">Add to Cart</button></p>
                    </div>

                </div>
            </div>

        </div>
        <%}%>
        </div>


        <!-- <div class="container" style="margin-top: 136px; background: #c7f5ac;">
            <% if(product){ %>
        <div class="card mt-4">
            <img class="" src="/image/<%= product.image %>" alt="">
            <div class="card-body">
                <h1><%= product.name %></h1>
                <p class="price">฿<%= product.price %></p>
                <p><%= product.description %></p>
                <p><button>Add to Cart</button></p>
            </div>
        </div>
        <%}%>
        </div> -->
    
</body>

</html>